class Nodo {
    constructor(isbn, titulo) {
      this.isbn = isbn; // Número ISBN
      this.titulo = titulo; // Título del libro
      this.izquierdo = null; // Hijo izquierdo
      this.derecho = null; // Hijo derecho
    }
  }
  